{
  "//": "Deno configuration for PalaGo Edge Functions. These are NOT part of the React Native TypeScript project — tsconfig.json excludes this directory, because Deno's jsr: specifiers, .ts import extensions and Deno global are all invalid there.",
  "imports": {
    "@supabase/supabase-js": "jsr:@supabase/supabase-js@2"
  },
  "lint": { "rules": { "tags": ["recommended"] } },
  "fmt": { "singleQuote": true, "lineWidth": 100 }
}
