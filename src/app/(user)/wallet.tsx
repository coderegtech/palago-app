import { PlaceholderScreen } from '@/components/common/placeholder-screen';

export default function WalletScreen() {
  return (
    <PlaceholderScreen
      title="Wallet"
      phase="Phase 9"
      description="Balance, mock top-ups and transaction history. Every entry is clearly labelled as a test transaction."
    />
  );
}
