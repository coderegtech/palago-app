import { PlaceholderScreen } from '@/components/common/placeholder-screen';

export default function TravelDataScreen() {
  return (
    <PlaceholderScreen
      title="Travel data"
      phase="Phase 7"
      description="Trip list with route, bus, crew, passenger counts and the passenger manifest."
    />
  );
}
