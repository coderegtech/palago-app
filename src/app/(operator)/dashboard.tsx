import { PlaceholderScreen } from '@/components/common/placeholder-screen';

export default function OperatorDashboardScreen() {
  return (
    <PlaceholderScreen
      title="Dashboard"
      phase="Phase 7"
      description="Today's trips, bookings, on-time rate and SOS alerts. All financial figures are test data."
    />
  );
}
