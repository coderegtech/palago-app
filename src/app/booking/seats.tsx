import { PlaceholderScreen } from '@/components/common/placeholder-screen';

export default function SeatsScreen() {
  return (
    <PlaceholderScreen
      title="Select seats"
      phase="Phase 4"
      description="Visual seat map with live availability, backed by the atomic reserve_seats RPC."
      showBack
    />
  );
}
