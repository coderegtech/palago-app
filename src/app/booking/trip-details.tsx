import { PlaceholderScreen } from '@/components/common/placeholder-screen';

export default function TripDetailsScreen() {
  return (
    <PlaceholderScreen
      title="Trip details"
      phase="Phase 4"
      description="Operator, bus, schedule, duration, fare and seat availability."
      showBack
    />
  );
}
